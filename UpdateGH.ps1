# Switch to first-dev
git checkout first-dev

# Stage all changes
git add .

# Verify changes are staged
git status

# Commit
git commit -m "Complete Mazerion v0.2.0 - 39 calculators with full GUI/TUI"

# Push to first-dev
git push origin first-dev