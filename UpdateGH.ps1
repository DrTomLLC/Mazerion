# Switch to first-dev
git checkout first-dev

# Stage all changesz
  git add .

# Verify changes are staged
git status

# Commit
git commit -m "Complete Mazerion v0.7.0 - Version Bump to 0.8.0"

# Push to first-dev
git push origin first-dev